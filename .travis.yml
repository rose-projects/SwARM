dist: trusty

branches:
    except:
    - pretest
    - elec

language: C

script:
    - make -C tests/RTLS/beacon
    - make -C tests/RTLS/robot

cache: ccache

addons:
    apt:
        packages:
        - gcc-arm-none-eabi
        - libnewlib-arm-none-eabi
