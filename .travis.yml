dist: trusty
sudo: false

branches:
  except:
  - pretest
  - elec

language: C

script:
    - make -C tests/blink/
    - make -C tests/led/

cache: ccache

addons:
    apt:
        packages:
        - gcc-arm-none-eabi
        - libnewlib-arm-none-eabi
