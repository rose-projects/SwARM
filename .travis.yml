dist: trusty
sudo: false

branches:
    except:
    - pretest
    - elec

language: C

script:
    - cd tests/blink/
    - make

cache: ccache

addons:   
    apt:
        packages:
        - gcc-arm-none-eabi
        - libnewlib-arm-none-eabi
