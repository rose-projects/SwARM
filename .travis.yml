dist: trusty
sudo: false

branches:
    except:
    - pretest
    - elec

language: C

script:
    - make -C robot/
    - make -C beacon/
    - make -C tests/blink/
    
cache: ccache

addons:
    apt:
        packages:
        - gcc-arm-none-eabi
        - libnewlib-arm-none-eabi
