branches:
    except:
    - pretest
    - elec

sudo: required

language: C

script:
    - make -C tests/ranging/
    - make -C tests/blink/

cache: ccache

addons:
    apt:
        sources:
        - sourceline: 'ppa:team-gcc-arm-embedded/ppa'
        packages:
        - gcc-arm-embedded
