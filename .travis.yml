branches:
    except:
    - pretest
    - elec

sudo: required

language: C

script:
    - make -C beacon
    - make -C robot

cache: ccache

addons:
    apt:
        sources:
        - sourceline: 'ppa:team-gcc-arm-embedded/ppa'
        packages:
        - gcc-arm-embedded
