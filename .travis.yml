branches:
  except:
  - pretest
  - elec

sudo: required

language: C

script:
    - make -C soft

cache: ccache

addons:
    apt:
        sources:
        - sourceline: 'ppa:team-gcc-arm-embedded/ppa'
        packages:
        - gcc-arm-embedded
