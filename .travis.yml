dist: trusty

branches:
    except:
    - pretest
    - elec

language: C

script:
    - make -C beacon
    - make -C robot

cache: ccache

addons:
    apt:
        packages:
        - gcc-arm-none-eabi
        - libnewlib-arm-none-eabi
